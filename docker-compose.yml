services:
  qdrant:
    image: qdrant/qdrant:v1.13.2  # Match client version
    container_name: qdrant_db
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
      - /opt/RAGProject/qdrant-config:/qdrant/config  # Mapping config directory
    restart: always

  app:
    build: .
    container_name: rag_api
    ports:
      - "8000:8000"
    depends_on:
      - qdrant
    volumes:
      - /opt/RAGProject:/app
      - /opt/RAGProject/uploads:/app/uploads  # Directory for AVRO file uploads
      - /opt/RAGProject/data:/app/data  # ✅ Mount data directory for AVRO files
    restart: always
    environment:
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6333

volumes:
  qdrant_data:
    name: qdrant_data
    external: true
