# RAGProject
RAG MVP Project

🔧 Installation & Dependencies
To set up this project, you'll need to install the required dependencies. Below are the steps to ensure everything runs smoothly.

📦 Required Dependencies
Package	Purpose
pandas	Data manipulation and preprocessing
numpy	Handling numerical operations, NaNs, and conversions
fastavro	Reading and processing AVRO files efficiently
sentence-transformers	Loading and using JinaAI’s jina-embeddings-v3 model for embeddings
torch	Required for deep learning models and inference
transformers	Provides support for Hugging Face models and tokenization
pytest	Running unit tests for validation
einops	Needed for jina-embeddings-v3 transformer model
torchvision	Required by transformers, used for deep learning operations
🛠 Installation Steps

Make sure you have Python 3.10+ installed. Then, install the dependencies using pip:

pip install pandas numpy fastavro sentence-transformers torch torchvision transformers pytest einops







